# CPU 策略說明

CPU 是可重現、可解釋的規則式控制器，不使用 AI、LLM、API、機器學習、Minimax、MCTS 或強化學習。實作位於 `src/cpu/`，策略函式只回傳決策，不直接修改 `GameState`；實際動作一律呼叫 `src/game/engine.ts` 的公開 action。

## 採購

每個合法產品計算固定效用：

```text
完成收藏 +12
+ 收藏進度每格 +3
+ 新縣市 +2
+ 新地區 +1
+ 新類別 +1
+ 目前產值 - 採購成本
```

一般採購後至少保留 3 採購金；完成收藏時至少保留 1。效用不大於 0 或資金不足就略過。平手依序比較效用、目前產值、成本（低者優先）、穩定產品 ID。

## 出售

只有在資金不高於 4，或產品相對基礎產值的市場溢價至少 2 時出售。若出售會讓已完成收藏失效，套用 -15 保護懲罰並略過；候選依出售效用、目前價值與產品 ID 穩定排序。

## 交通與事件

交通格逐一評估合法離島目的地的最佳可採購產品，沿用採購效用；沒有正效用候選就略過。事件效果由 engine 自動處理，CPU 不預判或偷看下一張市場卡。

## 生命週期

CPU 與真人共用擲骰、逐格移動、BoardCamera、抵達與回到全景流程。CPU 思考與決策延遲只屬 UI presentation；測試模式 20–60ms，正式模式約 250–450ms。CPU 回合期間真人按鈕鎖定，狀態透過 `aria-live` 公告，restart/離頁會清除所有 timer 與 camera lifecycle。
